<form method="post" action="{{ path('app_trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="w-5/12 h-14 bg-no-repeat bg-cover m-1 text-transparent">Delete</button>
    {# <button class="btn">
            <img src="images/trash-can.png" alt="trash can icon">
    </button> #}
</form>
